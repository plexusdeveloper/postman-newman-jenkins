{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run tests/newman-jenkins.postman_collection.json --reporters junit --reporter-junit-export newman.xml"
  },
  "author": "Rafael Calvo",
  "dependencies": {
    "newman": "^4.5.7"
  }
}
